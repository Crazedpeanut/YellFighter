using UnityEngine;
using System.Collections;

public class Clouds : MonoBehaviour {

	Vector3 startPos = new Vector3(-19.4f, 0f, 0f);

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
